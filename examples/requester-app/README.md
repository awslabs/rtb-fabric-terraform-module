# Requester App with Tags Example

Creates an RTB Fabric requester application with custom tags.

## Usage

1. Update `main.tf` with your AWS resource IDs
2. Run:
```bash
terraform init
terraform plan
terraform apply
```

## Resources Created

- 1 RTB Fabric Requester App with tags